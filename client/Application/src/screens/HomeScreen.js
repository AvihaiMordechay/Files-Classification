import { View, Text } from 'react-native';
import Header from '../components/Header';
const HomeScreen = () => {

    return (
        <View >
            <Header name={"אביחי"} />
        </View>
    );
};

export default HomeScreen;